interface:
  display_name: "Pre PR Review Loop"
  short_description: "Review, fix, and loop before PR handoff"
  default_prompt: "Use $pre-pr-review-loop to prepare this branch for PR and keep the human last in the loop."
policy:
  allow_implicit_invocation: true
